# ROADMAP.md - Milestones (checkboxes are the project's progress bar)
Each milestone = one PROMPTS.md implementation prompt = one branch = one gate. Order is dependency-true; no milestone requires rewriting a previous one.

- [x] **M0 Foundation online**: local stack boots, migrations applied (CI migration job green), /healthz + /config.json live locally AND on the droplet via CD, verify scripts pass. GATE: one-line change live on api.<domain> in <5 min.
- [x] **M1 Identity & shells**: auth (activation flow, login, JWT+refresh, roles), role-routed app shells (student/teacher/admin), landing page, logging+Sentry wired, frontend deploy step added to CD. GATE: three seeded users log in and see their shells on the real domain.
- [x] **M2 Institution, pools, people**: admin CRUD for teachers/students (CSV import), pools + members, activation issuance, self-serve teacher tier. GATE: import 10 students -> pool -> all can activate and log in.
- [x] **M3 Curriculum**: subjects, chapters, topics, ordered blocks (reorder), topic_edges (teacher links), materials upload (StorageProvider) + text-PDF extraction, publish flow, pool attach with snapshot+delta + "new members" banner. GATE: Anvi builds DIP with 2 chapters and publishes; Yash sees it.
- [x] **M4 Question banks & diagnostics**: AI gateway + providers + Content Store, per-topic bank generation into review queue, teacher review UI, diagnostic engine (stratified draw + weak-prereq slot), end-of-probe review screen, attempts + BKT + mastery(+history). GATE: fresh topic -> bank -> approve -> Yash's 5-question probe -> review screen -> mastery rows exist.
- [ ] **M5 Personalized learning**: session planner (prereq injection), segment generation (segment_shared cache), session player (recipe cards, instant practice reasoning), timeline events throughout, student timeline screen. GATE: weak-prereq student sees injected Transforms revision before Frequency Filtering; cache hit on second student.
- [ ] **M6 Teacher analytics**: Today view (stuck/cluster/ungraded signals + actions), Explorer drill-down, class/topic heat views, per-student detail + timeline, misconception clustering. GATE: seeded cohort produces a true cluster; teacher drills to the student who caused it.
- [ ] **M7 Assessments & assignments**: assessment blocks (gating, feedback modes, taking UI), assignment rubric builder, submission upload, review screen, return-for-revision, grades on timelines. GATE: checkpoint mid-chapter works both gated and open; one full assignment round-trip.
- [ ] **M8 Doubts & Explore**: typed doubt -> root-gap -> Socratic/direct streaming with material grounding; Explore with quota + global dedupe library. GATE: doubt about Frequency Filtering surfaces Transforms as root; "What are vectors?" asked twice = one generation.
- [ ] **M9 Mentor & guidance**: deterministic next-best-action + revision queue + weekly deltas, mentor cards, confidence decay job. GATE: guidance changes correctly after a mastery shift; decay resurfaces a stale topic.
- [ ] **M10 Admin & cost**: content manager (flagged artifacts queue), config viewer, AI cost dashboard (ai_invocations rollups), security flags wired end-to-end. GATE: flag an artifact as teacher -> appears in admin queue; cost per student renders.
- [ ] **M11 Hardening & demo**: e2e suite green, seed_demo complete, DEMO_RUNBOOK rewritten for StudySetu, backups cron live + one restore drill, load sanity (60 concurrent students), polish pass per DESIGN.md. GATE: two consecutive flawless runbook executions.
- [ ] **M12 (Phase 2 kickoff, post-launch)**: OCR intake, offline sync (events client_id/seq), AI pre-grading, syllabus import: each behind its existing flag.

Cut line: M9/M10 features degrade gracefully if time-boxed; M0-M8 are the product.
