# MEMORY.md - Living Project State (read FIRST every session; update LAST every session)
Format: replace the SNAPSHOT in place; append one line to the LOG. This is a state document, not a diary.

## SNAPSHOT (current truth)
**Project status:** M0 code-complete on branch `chore/m0-domain-integration` (PR #1, CI green, not yet merged/deployed). Zero business logic implemented.
**Product:** StudySetu - A Personalized Learning Platform for Every Student (name FINAL; see docs/BRANDING.md).
**Completed milestones:** none (foundation precedes M0; M0 itself not yet GATE-verified live).
**Current milestone:** M0 Foundation online (docs/ROADMAP.md) - code done, live-CD verification blocked on droplet bootstrap (see below).
**Next session objective:** once the droplet is bootstrapped and secrets supplied (see "what remains" below), merge PR #1, watch CD, verify https://studysetu-api.caffeineclause.tech/healthz + /config.json live, THEN tick M0's ROADMAP box and start M1.

**Architecture in one paragraph:** FastAPI modular monolith + React SPA on one DO droplet (Docker Compose: api+postgres; project-specific TLS/reverse-proxy removed - HTTPS is now a SHARED layer across every caffeineclause project, in a separate `caffeineclause-edge` repo, ADR-011) behind an owned domain (caffeineclause.tech, flat subdomains). Topic graph + BKT mastery + append-only events ledger (= the timeline) + Generated Content Store (lookup-before-generate) are the four load-bearing ideas. All behavior config-driven via config/*.yaml per subsystem. Full detail: ARCHITECTURE.md.

**Folder summary:** apps/api (backend; modules are docstring stubs; core/config+logging+main now real: pydantic validation, loguru+Sentry, CORS), apps/web (frontend shell; boots from /config.json; cross-origin API base URL plumbing added), db/migrations (0001-0006, VALIDATED: 32 tables), config/ (13 subsystem YAMLs, real domain), infra/ (compose dev+prod - prod no longer runs Caddy, Caddyfile is reference-only, deploy.sh, droplet-bootstrap.sh), prompts/ (5 stubs), scripts/ (migrate/seed-stub/backup/verify x2), e2e/ (spec plan), .github/workflows (ci: 3 jobs green on pnpm 9 + uv, deploy with rollback, path /home/deploy/studysetu).
**Important files:** docs/FEATURE_EXPLANATION.md (functional truth, APPROVED), docs/RULES.md, docs/DATABASE.md, docs/ROADMAP.md, docs/PROMPTS.md, docs/CONFIG.md, docs/DEPLOYMENT.md (rewritten for the shared-edge model), apps/api/app/core/config.py (sole config reader, now pydantic-validated), apps/api/app/ai/__init__.py (facade contract).

**Key decisions (ADR-001..011 in ARCHITECTURE.md):** self-hosted PG on droplet; polymorphic topics (subject|explore); chapter_blocks ordered flow; snapshot+delta pools; Content Store first-class; deterministic guidance; teacher-in-loop grading; item-bank diagnostics with end-of-probe feedback; ADR-011 shared multi-project edge layer (caffeineclause-edge repo) instead of per-project Caddy.
**Known issues:** GHCR image is private (repo is private) but nothing on the droplet is authenticated to pull it yet - `docker compose pull api` in deploy.sh will fail until either the GHCR package is made public (recommended: image contains no secrets, RULES #10) or a docker login step + PAT secret is added. **Deviations from docs:** none beyond what ADR-011 documents.
**Current configuration notes:** domain is real and live: caffeineclause.tech, droplet 64.227.177.181, DNS confirmed (root+wildcard A, AAAA, www CNAME). Flat subdomains: studysetu.caffeineclause.tech (frontend, served at M1) / studysetu-api.caffeineclause.tech (backend). ai.embedding_dim=768 must still match the chosen embedding model before M4.
**What remains before M0's GATE is demonstrably true (droplet-side, human-executed - see session report for the full numbered list):** run infra/droplet-bootstrap.sh as root; place /home/deploy/studysetu/docker-compose.yml + .env (production DATABASE_URL must point at the `postgres` service, not localhost) + infra/deploy.sh; create/push the caffeineclause-edge repo from the local scaffold at ../caffeineclause-edge and run it on the droplet; resolve the GHCR-pull-auth gap above; set GH secrets DROPLET_USER + DROPLET_SSH_KEY (DROPLET_HOST is already set); merge PR #1; watch the CD run.

## LOG (append one line per session: date | milestone | one-line outcome)
2026-07-15 | foundation | StudySetu foundation v2 finalized: schema validated (32 tables), skeleton, config system, docs, prompts, verify scripts; ZIP = repo (single source of truth).
2026-07-17 | M0 | Fixed foundation boot bugs (pydantic config validation, loguru+Sentry, CORS, broken Dockerfile/CI/pytest/pnpm), wired real domain caffeineclause.tech via a new shared-edge architecture (ADR-011, caffeineclause-edge sibling repo scaffolded not pushed), verify_local green + CI 3/3 green on PR #1; live CD verification blocked on droplet bootstrap (human-executed, script provided).
