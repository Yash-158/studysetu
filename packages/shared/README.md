# packages/shared
src/api-types.ts: generated from the FastAPI OpenAPI spec (regen script added at M1; never hand-edit).
testdata/bkt_vectors.json: golden BKT input/output vectors so the Python implementation and any TS mirror agree (authored at M4).
