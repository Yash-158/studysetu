# apps/web - Frontend PWA (React 18 + TS + Vite)
Created during SETUP_STEPS Step 5 by Claude Code. Owns: student UI, evaluator UI, Dexie offline layer, service worker.
Never contains: API keys, provider SDKs, business math not mirrored+tested against packages/shared vectors.
