# Loaded by `vite build` only (not `vite dev`). Frontend and API are cross-origin in production
# (flat subdomains, config/deployment.yaml is the backend-side source of truth for this same value).
VITE_API_BASE_URL=https://studysetu-api.caffeineclause.tech
