# prompts/ - versioned LLM prompt files (RULES #6)
One file per AI task; filename = task name in config/ai.yaml chains. Header of each file: version (feeds prompt_version in cache keys), model notes, output contract. Prompts are code: change = PR.
